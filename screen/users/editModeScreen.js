import React, { Component } from 'react';
import { View, Text } from 'react-native';

class editModeScreen extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> editModeScreen </Text>
      </View>
    );
  }
}

export default editModeScreen;
