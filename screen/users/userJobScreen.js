import React, { Component } from 'react';
import { View, Text } from 'react-native';

class userJobScreen extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> userJobScreen </Text>
      </View>
    );
  }
}

export default userJobScreen;
